#device: trv_bedroom
  location: bedroom
  entities:
    battery: 
      entity_id:: binary_sensor.trv_bedroom_battery
    trv: 
      entity_id: climate.trv_bedroom
      attr: #attributes for entity trv: climate.trv_bedroom
        hvac_modes: hvac_modes 
        min_temp: min_temp
        max_temp: max_temp
        current_temperature: current_temperature
        temperature: temperature
        occupied_heating_sepoint: occupied_heating_sepoint
        system_mode: system_mode
        friendly_name: friendly_name
        supported_features: supported_features
    temperature_offset:
      entity_id: number.trv_bedroom_local_temperature_offset
    hvac_action: 
      entity_id: sensor.trv_bedroom_hvac_action
    valve_position: 
      entity_id: sensor.trv_bedroom_valve_position
    away_mode: 
      entity_id: switch.trv_bedroom_away_mode
    child_lock: 
      entity_id: switch.trv_bedroom_child_lock
    frost_protection: 
      entity_id: switch.trv_bedroom_frost_protection
    scale_protection: 
      entity_id: switch.trv_bedroom_scale_protection
    schedule_enable: 
      entity_id: switch.trv_bedroom_schedule_enable
    window_detection: 
      entity_id: switch.trv_bedroom_window_detection
    firmware: 
      entity_id: update.trv_bedroom_firmware